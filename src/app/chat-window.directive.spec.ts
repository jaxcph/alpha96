import { ChatWindowDirective } from './chat-window.directive';

describe('ChatWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatWindowDirective();
    expect(directive).toBeTruthy();
  });
});
